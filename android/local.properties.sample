# Copy to android/local.properties (gitignored).
# Path to your Android SDK:
sdk.dir=/home/USER/Android/Sdk

# Base URL of the SCA backend the APK should talk to (needs a trailing slash).
# Defaults to the production host if unset. Must be HTTPS for a release build.
sca.baseUrl=https://ctn-sca.ddns.net/
